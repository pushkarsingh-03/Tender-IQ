{
  "name": "tenderiq",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"bun run server.ts\" \"bun run --cwd client dev\"",
    "server": "bun run server.ts",
    "client": "bun run --cwd client dev",
    "migrate": "bun run migrate.ts",
    "build": "bun run --cwd client build",
    "db:dump": "sqlite3 tenderiq.db .dump > db_snapshot.sql && echo 'DB dumped to db_snapshot.sql'",
    "db:restore": "sqlite3 tenderiq.db < db_snapshot.sql && echo 'DB restored from db_snapshot.sql'"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
